
import 'package:flutter/material.dart';

class SignatureFlowScreen extends StatelessWidget {
  const SignatureFlowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fluxo de Assinaturas')),
      body: Center(
        child: Text('Tela para fluxo de assinaturas'),
      ),
    );
  }
}
    